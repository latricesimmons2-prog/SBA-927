[project]
name = "sba-927"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nltk>=3.10.2",
    "numpy>=2.5.2",
    "torch>=2.13.0",
    "transformers>=5.15.1",
]

[project.scripts]
sba-927 = "sba_927:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
